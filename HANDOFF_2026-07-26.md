# カタモン 引き継ぎ(2026-07-26)

前回の引き継ぎは `HANDOFF_2026-07-24.md`(Codex作成)。**こちらが最新の正**。
差分は「キャラ15体化 → オンラインランキング完成 → 内部モデルのN人化 → Stage 2a(視点の切り離し) → **Stage 2b(BroadcastChannelによる2タブ対戦)完了**」。

---

## 0. 最初にやること

```bash
cd "C:/Users/futsa/Documents/Codex/2026-07-22/claudecode/katamon"
git pull --ff-only
git status -sb
```

作業前に必ず `origin/master` と一致しているか確認すること。`reset --hard` は未保存の変更を消すため、通常の同期には使わない。

- 唯一の正: `https://github.com/futsalife24-bot/katamon`(ブランチは **master**)
- 公開先: https://futsalife24-bot.github.io/katamon/
- 2026-07-26 引き継ぎ時のGitHub基準点: **`de914f8`**、push 済み。確認開始時の作業ツリーはクリーン
  - (旧記載 `1f78c23` は Stage 2b 時点のもの。その後 `39898ea` Firebase 1v1 と `de914f8` を追加してある)

### Codexとの担当整理
本開発リポジトリは `C:\Users\futsa\Documents\Codex\2026-07-22\claudecode\katamon`。
2026-07-26、Claude Codeの利用上限到達を受けて、ユーザーが**Codexへ明示的に再移管**した。
以後はこの本開発リポジトリで作業し、GitHubの `master` を唯一の正とする。OneDrive側のコピーは確認用であり、開発元にしない。

---

## 1. 何を作っているか

**カタモン(カタパルトモンスター)** — 縦画面スマホブラウザ向けのターン制砲撃ゲーム。ポトリス/ガンバウンドM系。

- **単一の `index.html`(約8,600行)**。バニラJS + Canvas 2D、全コードが1つのIIFEの中。ビルド工程なし
- PWA(`manifest.webmanifest` + `sw.js`)。**`sw.js` の `CACHE_VERSION` はデプロイのたびに上げること**(現在 **v17**)
- 仮想座標系 VW=540 / VH=960 / STAGE_W=1440
- キャラ15体、ステージ複数、フリーモード、連勝サバイバル、中断再開、オンラインランキング
- **`tests/` にブラウザ不要の検証ハーネスがある**(→ §7)

---

## 2. 絶対に守るルール(ユーザーとの取り決め)

1. **`git push` は必ず事前に許可を取る。** コミットまでは進めてよい
2. **成果物をファイル添付で渡さない。** ユーザーのリモートコントロール画面は添付を受け取れない。**必ずGitHub PagesのURLか、GitHubのblob URLで渡す**
3. 日本語で応対する

---

## 3. いま完成している主要機能

| 機能 | 状態 |
|---|---|
| 地形生成・破壊(ピクセルマスク)、弾道物理、風、燃料移動 | 完成 |
| キャラ15体 + 必殺技 | 完成(名前は仮のまま。正式名称は未定) |
| 連勝サバイバル・ボス戦(10連勝ごと)・フリーモード | 完成 |
| 中断再開(localStorage) | 完成。形式は **v4** |
| オンラインランキング(GAS + スプレッドシート) | 完成・稼働中 |
| 敗北時の「◯位 入賞!」表示 / ランキングへの使用キャラ表示 | 完成(2026-07-26) |
| 内部モデルのN人化(オンライン対戦の下地) | 完成(2026-07-26) |
| **Stage 2a: 視点の切り離し(席の概念)** | **完成(2026-07-26)** |
| **Stage 2b: BroadcastChannelによる2タブ対戦** | **完成・公開版で目視確認済み(2026-07-26)** |
| **勝利画面「中断してタイトルへ」** | **完成(2026-07-26)** |
| Stage 3: Firebase外部回線オンライン1vs1 | **実装済み・実機QA継続中（actionId相関を追加。最終確認はroot担当）** |

---

## 4. オンラインランキングの運用

- GASウェブアプリ + スプレッドシート。ユーザーが**ゲーム専用のGoogleアカウント**を作ってデプロイ済み
- スクリプト: `gas/ranking.gs`(公開リポジトリにある。**コンテナバインド型なのでシートIDを持たず安全**)
- 手順書: `gas/セットアップ手順.md`(初回セットアップと**再デプロイ手順**の両方が書いてある)
- デプロイURLは `index.html` の `RANKING_API`(**index.html:6849**)に直書き。**再デプロイしてもURLは変わらない**
- 順位 = 連勝数が主、同数なら与ダメ効率。端末IDごとに自己ベストのみ上書き。月次で期間切り替え
- 送信は `applyStreakResult()` の敗北分岐(連勝が途切れた瞬間)。フリーモードと0連勝は送らない
- 失敗時はlocalStorageに貯めて起動時に再送(`flushPendingScore`)
- **名前入力だけHTMLのオーバーレイ**(`#nameOverlay`)。ゲーム唯一のDOM UI
- **GASの応答は2〜3秒かかる。** 待っている間の表示は必須

### ⚠️ ユーザー側に残っている作業(2026-07-26時点で未確認)
1. **`ranking.gs` の再デプロイ。** `character` 列(G列)を足したので、これをやるまでランキングのキャラ欄が全部「−」になる
2. シートのリセット。最後に見た時点で `ふつサ 11連勝` と `メロソ 1連勝` の2行が残っていた

---

## 5. データモデル — units配列と「席」

### 5-1. 内部モデルのN人化(2026-07-26)

`player` / `cpu` の2固定オブジェクトを進行ロジックから外し、`units` 配列に集約した。**UIは1vs1のまま。**

| 概念 | 場所 |
|---|---|
| `units` 配列 | index.html:1968 |
| `unitById` / `teamOfOwner` / `teamAlive` / `nearestEnemy` / `creditDamage` | index.html:1970〜 |
| `turnOrder` + `activeIndex` / `activeUnit()` / `isLocalTurn()` | index.html:2342〜2348 |
| `buildSnapshot()` / `migrateSnapshot()` / `applySnapshot()` | index.html:2690 / 2714 / 2752 |
| `startTurn()` / `endTurn()` | index.html:5007 / 5022 |

**ユニットが持つフィールド**
- **`id`** … 弾の `owner` や通信で個体を指す不変のキー(`'p1'` / `'e1'`)。旧 `side` を置き換えたもの
- **`team`** … 陣営(`'player'` / `'cpu'`)。**勝敗判定・味方判定はすべてこれで行う**
- **`control`** … `'local'`(この端末の操作)/ `'cpu'`(AI)/ **将来 `'remote'`(相手端末)**

**押さえておくべき点**
- **弾の `owner` はユニットID。** 陣営を見るには `teamOfOwner(owner)` で引き直す
- 総ターン数は `TURNS_PER_UNIT × 人数`、風の切り替えは `turnCount % (人数 × 2)`
- 倒れたユニットの手番はスキップする(1vs1では通らないが2vs2で必要)
- 中断セーブは **v4**。`v3` は `migrateSnapshot` が `units` 形式へ寄せる
- **`player` / `cpu` は `units[0]` / `units[1]` の別名として残してある。** キャラ選択・初期配置・ランキングまわりだけがまだ直接見ている

### 5-2. Stage 2a: 席(seat)の切り離し(2026-07-26完了)

オンライン対戦ではゲスト側が **`e1`(画面右)に座る**。UIが `player` / `cpu` を positional に見ていると全部壊れるので、
**入力・HUD・カメラ・結果画面をすべて席側の関数へ通した。**

| 関数 | 場所 | 役割 |
|---|---|---|
| `localUnitId` | index.html:1979 | この端末が座っているユニットID |
| `localUnit()` / `foeUnit()` | index.html:1980 / 1981 | 自席 / 相手 |
| `localWon()` | index.html:1987 | `winner`(陣営名)を席から見て勝敗に直す |
| `turnOwnerLabel(u)` | index.html:1989 | 「あなた/CPU/相手」の出し分け |
| `setLocalSeat(id)` / `setOnlineSeat(id)` | index.html:2000 / 2001 | 席を移す。オンライン時は自席を `local`、相手を `remote` にする |

- **開発用の入口: `?seat=e1`** でURLから席を移せる。タイトルには何も出していない
- `playerFireworkInFlight` は **`localFireworkInFlight`(index.html:2204)** に改名(所有者が席のユニットになったため)
- **`isPlayerTurn()` は削除した。** 手番の判定は `isLocalTurn()`(席)か `activeUnit().team`(陣営)を使い分けること

**`team` のまま残してあるもの(これらは「プレイヤー陣営」の話であって席の話ではない)**
キャラ選択・フリーモード設定・`resetMatch` の初期配置・連勝・ランキング送信・スプライトの向き。

> ⚠️ **Stage 3 での宿題**:連勝とランキングは `team === 'player'` で数えている。
> 席を `e1` にすると「相手が勝つと自分の連勝が増える」ことになる。
> オンライン対人戦はランキングに含めない方針(→ §6)なので、**Stage 3 では対人戦がこの経路を通らないようにすること。**

---

## 6. Stage 2b完了状況と次にやること

全体計画は **`C:\Users\futsa\.claude\plans\declarative-beaming-sutton.md`** にある。必ず先に読むこと。

方針の要点(ユーザー承認済み):
- **リアルタイム同時対戦**が希望。置き手紙型は不採用
- **2vs2は将来。今回は1vs1**
- 方式は **「行動側権威 + ターン境界スナップショット」**。ロックステップは採らない(可変dt・非シード地形・JSエンジン差でデシンクするため)
- 通信基盤の第一候補は **Firebase RTDB**(REST + SSEでSDK不要、単一HTMLを崩さない)、フォールバックはGAS
- **Firebase無料枠で新規RTDBが作れるかは未確認。着手前に実機で確かめること**
- **対人戦の結果はランキングに含めない**(勝ちの譲り合いの温床になるため)

### 6-1. Stage 2b: BroadcastChannelループバック対戦 — 完了

- 通信は `send / onMessage / close` の差し替え可能なインターフェース。Stage 3では `makeTransport` の実装をFirebase RTDB向けへ差し替える
- プロトコルは `PROTO_VERSION`(index.html:2855)。**Stage 3 で `2` に上がっている**。不一致は「アプリを更新してください」で停止
- 実装済みメッセージ: `join` / `hello` / `start` / `ready` / `fire` / `boom` / `state` / `result` / `bye`
- カットイン・飛翔・落下・`pendingShot` 中は進行メッセージをキューへ積み、二重発射を防止
- 開発用入口: `?loopback=host` / `?loopback=guest`。人工遅延は `&netdelay=1500`、ロスは `&netloss=0.1`
- 対人戦は連勝・ランキング・中断セーブへ混ぜない
- `node tests/loopbacktest.js`: **93 passed, 0 failed**
- 2026-07-26 Codex目視: 公開版2タブを1.5秒遅延で接続し、席別HUD、同一弾道、着弾後の同一地形、手番切り替え、ブラウザエラー0件を確認

### 6-2. Stage 3へ入る前の順番

1. **Firebase Spark無料枠で新規Realtime Databaseを作成できるか実機確認する。** 作成不可ならGAS案を再検討
2. Firebaseを使える場合、セキュリティルール・ルーム有効期限・6文字以上の合い言葉・同時キャラ公開・先攻決定方式を確定
3. Stage 3(合い言葉ロビー + オンライン1vs1)を実装
4. Stage 4(切断・画面ロック・再入室)を実装してからStage 5(2vs2)へ進む

### 6-3. Stage 4の再見積もり(2026-07-26)

Stage 4は付け足しではなく、**Stage 3本体の0.8〜1.2倍程度を見込む大タスク**として扱う。

| 項目 | 規模 | Stage 3で先に仕込むもの |
|---|---|---|
| heartbeat / presence / 「相手が離席中」表示 | 中 | 接続状態と最終通信時刻 |
| 手番タイムアウト・パス／投了 | 中 | サーバ時刻基準の期限 |
| 画面ロック・再入室・状態再同期 | 大 | ルームID・席・最新スナップショットの永続化 |
| 同一端末2タブ・3人目・古い接続の追い出し | 中〜大 | クライアントIDと接続世代 |
| 切断時の不戦勝・部屋の掃除 | 中 | 明示的な部屋状態と有効期限 |

特に**再入室**と**複数接続の排他**が見積もりの山。Stage 3でデータ構造を決める時点から、後付けにならないよう含める。

---

## 7. 検証のやり方

### 7-1. まず `tests/` を回す(ブラウザ不要・これが主力)

`index.html` の `<script>` を抜き出し、Canvas / WebAudio / DOM をスタブしたNode上で実行する。
実際にポインタイベントを流して1試合まるごと進められる。**ビルドも依存パッケージも不要。**

```bash
node tests/seattest.js p1
node tests/seattest.js e1
node tests/regressiontest.js p1
node tests/regressiontest.js e1
node tests/resulttest.js
node tests/stage3test.js
node tests/loopbacktest.js
```

| ファイル | 中身 |
|---|---|
| `tests/seatharness.js` | スタブとフックの土台。`globalThis.__kt` にゲーム内部を露出する |
| `tests/seattest.js` | 席の切り離し。入力・HUD・弾のowner・勝敗(19項目) |
| `tests/regressiontest.js` | CPU戦の完走・中断再開のラウンドトリップ・フリーモード(20項目) |
| `tests/resulttest.js` | 結果画面の「タイトルへ戻る」(26項目) |
| `tests/peer.js` | ループバック対戦の1タブ分。親プロセスから通信を注入する |
| `tests/loopbacktest.js` | host/guest、遅延、ロス、必殺、受信キュー、プロトコル不一致(93項目) |
| `tests/stage3test.js` | Firebase経路。匿名認証・commit/reveal・actionId相関・conditional PUT(49項目) |

- **検証フックは `seatharness.js` が実行時に注入する。`index.html` には一切入れない。** 消し忘れが原理的に起きない
- **カットイン中はゲーム進行が止まる。** `cutIn` を外から `null` にすると `showCutIn` の `onDone`(タイムアップ判定など)が道連れに消えて試合が終わらなくなる。飛ばさず `settle()` で経過させること
- **地形は毎回ランダム。** 移動で崖から落ちて `grounded` が外れることがあるので、「撃てる状態になるまで待つ」形で書くこと
- 新しい機能を足したら、ここにテストを足してから進めること

### 7-2. ブラウザでの目視(できるときだけ)

- **プレビューサーバーのルートは `カタモン` フォルダそのもの。** 正しいURLは `http://localhost:8944/`。`/カタモン/index.html` は空ページを返すが**コンソールエラーが出ない**ので「エラーなし=正常」と誤診する。最初に必ずページが載っているか確かめる
- `.claude/launch.json` に `katamon`(port 8944)の設定がある。**ただし `preview_start` はプライマリ作業ディレクトリ側の `.claude/launch.json` を読む**ので、`デスクトップ` を作業ディレクトリにしている場合はそちら側にも要る
- Claude Code側ではブラウザペインが非表示で目視できなかったが、**同日Codexのアプリ内ブラウザでStage 2bの2タブ試射まで確認済み**。ブラウザが使えない環境では §7-1 のNodeハーネスへ切り替える
- **タブがフロントに居ないと rAF が止まる。** 物理やCPUターンを進める検証の前に `tabs_select` でタブを前面に
- **`javascript_tool` は30秒でタイムアウトする。** 長い自動進行は非同期ドライバを起動して結果を `window.__res` に置き、別呼び出しでポーリングする
- **Service Worker がキャッシュを返す。** 検証前に `getRegistrations().unregister()` と `caches.delete()`、URLにクエリを付けて読み直す
- 構文チェック: `<script>` の中身を抜き出して `node --check` にかける。GASは `.gs` を `.js` にコピーしてから
- **GASのロジックテストは `scratchpad/gastest.js`。** スプレッドシートのAPIをスタブで模してある。**"2026-07" が日付へ自動変換される挙動を再現しているのが肝**(これが無いと過去のバグを捕まえられない)。25項目通る

---

## 8. 踏んだバグ・注意点(再発しやすい順)

- **`regressiontest.js` の「保存前にクレーターができている」(`tests/regressiontest.js:86`)はランダム地形・弾道に依存する不安定テスト。** 2026-07-26の引き継ぎ確認では `p1` で7回中1回だけ落ち、残り6回は20/20合格(`e1` は落ちず)。**落ちてもコードの異常ではない。** 継続的に回すなら乱数固定か、クレーターを確実に作るテスト入力へ変更する
- **スプレッドシートは `"2026-07"` のような文字列を日付へ自動変換する。** 書き込んだ値と読み戻した値が一致せず、行の検索も期間の絞り込みも全部失敗する。該当列は `setNumberFormat('@')` でテキスト固定し、読み取り時も `normalizePeriod` で正規化する
- **`p.gravityMul || 1` で 0 が 1 に化けていた。** 0を渡す設計を足す時は `!= null` 判定にすること。同種の `|| 1` が他にもある
- **カットイン中に「弾がなく着地済みなら手番交代」が発火してターンが飛ぶ。** 判定に `!pendingShot` が必須
- **カットインの `onDone` を消すと試合が終わらなくなる。** `showCutIn('タイムアップ!', …, () => judgeByHp())` のように進行の続きがコールバックに乗っている
- **`craterHistory` がフレームレートに比例して膨らむ**問題は修正済み(距離間隔で刻む方式)。同種の「毎フレーム記録」を足す時は注意
- **闘技場の鉄球の当たり判定は `simTimeMs`(dtの積算)を使う。** `performance.now()` に戻すと端末の起動時刻で当たり外れが変わる。**描画側も同じ時刻を使わないと見た目と判定がズレる**
- **背景透過は「フラッドフィル + 距離変換クロージング」が正解。** 単純なフラッドフィルだけだと背景のグラデを伝って被写体内部の黒いパーツまで食われる。手順は `scratchpad/cutout.py`。**検証はマゼンタ背景に合成して自分の目で見ること**
- **軽量モデルのエージェントに画像処理を任せた時、「目視で問題なし」の報告が誤りだった。** 画像品質の最終判断は自分で見ること
- **ユーザーがブラウザのアドレスバーからGASのURLをコピーすると `script.googleusercontent.com/macros/echo?...` が取れてしまう。** 正しいのは「デプロイを管理」に出る `/exec` で終わるURL
- **Bashツールに PowerShell の here-string(`@'…'@`)を渡すと文字列がそのまま混ざる。** コミットメッセージは `git commit -F -` + heredoc で渡すこと

---

## 9. ユーザーから出ている調整候補(未着手・言われたら対応)

- `TURN_SETTLE_SEC`(0.8)、`MATCH_END_HOLD`(1.0)/ `MATCH_END_BANNER`(1.3)
- ダメージ数字の最大サイズ(44px)
- 花火の炸裂数・大きさ(拡散弾1本につき3回)、至近距離での自爆ダメージ(最大42)
- キャラの正式名称(いまは全部仮名)
- 「◯位 入賞!」が出るまでの2〜3秒の待ち。**通信時間なので短縮できない。** 気になると言われたら演出の見せ方で誤魔化す方向になる

---

## 10. 未確認のまま残っていること

- 勝利画面の **「中断してタイトルへ」のラベルが210px幅のボタンに収まっているか**は未目視
- Stage 2bの**決着画面そのもの**は公開版で未目視。ただし自動テストでは両端末の勝敗・HP一致と、席基準の `localWon()` を確認済み
- **実機での「◯位 入賞!」とキャラ表示**(GAS再デプロイ待ち)
- **実機のIMEでの日本語名入力**(こちらの環境では検証できない)
- Android の戻るボタン / iOS のスワイプバック(コードは入っているが実機未確認)
- ランキングの「通信エラー画面」と「まだ誰も記録がない」状態は、**スタブでは確認したが実サーバー相手には出していない**
- BGM、キャラ正式名称、2vs2

---

## Stage 3 最終引き継ぎ（2026-07-26・この節を正本とする）

- リリース済み：`master`へ `39898ea` (`feat: add Firebase online 1v1`) をpush済みで、`origin/master`と一致している。GitHub Pagesは [https://futsalife24-bot.github.io/katamon/](https://futsalife24-bot.github.io/katamon/) 。Firebase project／Realtime Database／本番ルールも公開・反映済み。
- Stage 3の要点：Firebase匿名Auth、8桁room、REST送信＋SSE受信、commit/reveal、48桁`actionId`相関、固定push IDへのconditional PUT（重複時照合・再送）、`ping` heartbeatと可視時間timeoutを実装。オンライン対戦はランキング・中断セーブの対象外。
- 自動テスト：**207/207 passed**（`seattest` 19、`regressiontest` 20、`resulttest` 26、`stage3test` 49、`loopbacktest` 93）。
- 本番スモークQA：2026-07-26に匿名認証2者、部屋作成、guest claim、`ping`、`actionId`付き`fire`、`actionId`欠落の拒否（401）、既存message上書き拒否（401）、member read、guest delete拒否（401）、host cleanup、削除後read拒否をすべて確認。仮部屋は削除済み。
- セキュリティ監査：P0/P1/P2なし。なおクライアント権威のカジュアル対戦なので、完全なチート耐性にはserver-authoritative backendが必要。
- 帰宅後TODO：物理2端末、別回線、画面ロック／バックグラウンド復帰、音・UIを確認。Pages反映待ちやService Worker v17のキャッシュが残る場合は再読込する。
- 次の開発優先順位：まずStage 4（切断・寝落ち・画面ロック・再入室）の見積りを見直す。Stage 3の「合い言葉コピー＋オンライン1vs1」は今回実装済みなので、計画書は未着手扱いをやめて進捗・残QAへ整理する。

### 引き継ぎ後の確認ログ

**2026-07-26 / Claude Code(引き継ぎ受領時)**

同期: `git merge --ff-only origin/master` で `1f78c23` → **`de914f8`** へ更新。作業ツリーはクリーン、`origin/master` と一致。
(OneDrive側のコピーが3コミット遅れていた。本開発リポジトリはCodex側だが、そちらは所有者が `CodexSandboxOffline` のためこのユーザーからは `git` で読めない)

自動テスト再実行:

| コマンド | 結果 |
|---|---|
| `seattest.js p1` / `e1` | 19 / 19 passed, 0 failed |
| `regressiontest.js p1` | **1回目 19 passed / 1 failed** → 再実行6回すべて 20 passed |
| `regressiontest.js e1` | 20 passed, 0 failed |
| `resulttest.js` | 26 passed, 0 failed |
| `stage3test.js` | 49 / 49 passed |
| `loopbacktest.js` | 93 passed, 0 failed |

**合計 207/207**(引き継ぎ書の数え方に合わせ、seattest と regressiontest は席ごとに重複計上しない)。
唯一の失敗は既知の不安定テスト `tests/regressiontest.js:86`(→ §8)。**コードの異常ではない。**

実機QA(物理2端末・別回線・画面ロック/バックグラウンド復帰・音/UI): **未実施。rootの帰宅後作業。結果が出たらこの表の下に追記すること。**

### Claude Codeへ渡す最初の指示

```text
まず git pull --ff-only、git status -sb、git rev-parse --short HEAD を確認してください。
次に自動テストを再実行し、帰宅後の実機QA（物理2端末・別回線・画面ロック/バックグラウンド復帰・音/UI）の結果をこのHANDOFFへ反映してください。
勝手にpushはしないでください。
```

## 11. Stage 3: Firebaseオンライン1V1（2026-07-26 実装・実機QA継続中）

- タイトルに **ONLINE 1V1** を追加。HTMLオーバーレイのロビーで部屋作成・8文字合言葉参加・モンスター選択・コピー/共有・キャンセルを行う。
- Firebase SDKは使わず、Identity Toolkitの匿名認証REST、Secure Tokenの期限前更新、RTDB REST送信 + EventSource(SSE)受信で実装。認証トークンはログに出さない。
- 部屋は `/rooms/{8文字}`。ホスト作成時は `protocol: 2`、サーバー時刻の `createdAt`、最大2時間の `expiresAt` を保存し、ゲストは `guestUid` を一度だけclaimする。
- 既存の `?loopback=host|guest` はBroadcastChannel経路のまま残した。実オンラインだけFirebase transportを使う。
- キャラは commit/reveal（SHA-256、暗号学的乱数nonce）。両者のhash検証後、ホストが `p1=host / e1=guest` のスナップショットを作成する。先攻は両nonceと部屋コードのhashから決めるのでホスト固定ではない。
- `database.rules.json` を追加。デフォルト拒否、参加者かつ期限内だけ読取、作成/host削除、guest claim一回、送信者一致の追記専用メッセージを記述した。~~**Firebase Consoleへのルール反映はまだしていない。**~~ → **正本(§Stage 3 最終引き継ぎ)では「本番ルールも公開・反映済み」。この節の記述は古い。**
- Service Workerキャッシュは v17。追加テストは `node tests/stage3test.js`。
- Firebase受信スナップショットは地形480列・クレーター・ユニット順/数値・風・カメラなど `applySnapshot` が読む形を完全検証する。stateではさらに開始時地形/テーマ/キャラ/maxHp/turnCountを固定し、HP・座標・燃料・必殺・クレーター数は実フレーム差の小さい範囲だけ許容する。解決待ち中は5秒キュー強制適用をしない。画面非アクティブ時に `state/result` が先着しても、48桁hexの `actionId` で対応する `fire` が来るまで保持し、送信者・actionIdの両方が一致した時だけ解決後に適用する。単独/不一致は拒否し、同一actionIdのresult再送だけは無害化する。
- SSEのpush keyは部屋寿命中すべて保持して重複を無害化する。匿名認証／トークン更新のIDトークンJWTから発行時刻`iat`と有効期限`exp`を検証し、受信時の端末時計との差をFirebaseサーバー時刻オフセットとして保持する。部屋期限と固定の数値`sentAt`はこの推定時刻で作り、JWTの時刻情報が不正なら開始しない（`.info/serverTimeOffset`のREST取得は使わない）。同じIDへ`if-match: null_etag`付きPUTを行い、通信結果が不明な場合は同じパスを最大3回試す。412時に既存本文を正規化して照合するので、二重送信と別メッセージ上書きを防ぐ。全試行失敗時は送信キューを停止して対戦を終了する。
- 非アクティブ中に`state`/`result`が`fire`より先に届いた場合の保留は、可視時間だけを15秒まで数える（隠れている間は停止）。対応`fire`が来なければ、未対応の状態を適用せず接続を終了する。
- `playing`中のFirebase対戦は10秒ごとに副作用なしの`ping`を送る。妥当な相手パケットを受けるたびに生存確認を戻し、可視状態で35秒間何も届かなければ対戦を中断する。非表示中は送信・タイムアウト加算を止めるため、送信側の全再送失敗で相手に`fire`自体が届かないケースも永続待機にならない。

実機で確認すること：`database.rules.json` をFirebase Consoleへ反映済みかを確認してから、別端末/別回線で「作成→共有→参加→commit/reveal→先攻→双方から一発→退出」を確認する。特に非アクティブ側を数十秒置いたあとでも、先着した `state/result` が対応する `fire` と結び付いて復帰することを確認する。匿名認証・SSEを広告ブロッカー/省電力モード下でも確認する。**Stage 3のライブQA最終判定はrootが行うまで保留。** ~~GitHubへの公開は行っていない。~~ → **公開済み。** `39898ea` は `origin/master` に入っており、2026-07-26 に `de914f8` まで確認した。

オンライン対戦は行動側権威のカジュアル対戦であり、Firebase RESTを改造したクライアントを完全には防げない。Firebase経路では不正な形式・手番外のfire・未確認のstate/result・ローカル物理結果と食い違うresultを拒否し、オンライン結果はランキング対象外にしている。完全な不正防止にはサーバー審判が必要。
