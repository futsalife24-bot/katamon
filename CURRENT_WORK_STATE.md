# カタモン 現在作業状態

最終更新: 2026-08-18 / GitHub正本: `futsalife24-bot/katamon` の `master`

## 現在の公開版

- 最新版は **v2.0.77**。全BGM先読み・サウンドテスト化とクール=カイ連戦BGM継続。

## v2.0.77 クール=カイ連戦BGM継続

- クール=カイの「つぎのバトルへ」では専用BGMを先頭へ戻さず、再生位置を継続する。
- タイトルへ戻った後の新しい対戦は、従来どおり専用BGMを先頭から再生する。

## v2.0.76 BGMサウンドテスト

- 起動ローダーで全BGMを先読みし、進捗へ反映。おまけを全BGM一覧の試聴画面へ変更。

- Content Studio: v0.5.0をv2.0.76へ統合。既存17体対応。

## v2.0.75 ローダー配置

- 回転する円盤石を約半分に縮小して上へ移動。
- ロード文章とゲージ上キャラが重ならないよう、ゲージとキャラを下へ配置。

## v2.0.72 ローダー

- 石壁を0〜100%化し、ランダムキャラが揺れながら進む演出を追加。

## v2.0.71 プリズムビーム

- 地形破壊なし・敵貫通・最大4回反射・1320px射程のレーザーへ変更。

## v2.0.70 クール=カイ演習画像

- 演習選択の透明余白を専用切り出しで調整。

## v2.0.69 初回BATTLE準備とデバフ状態表示（2026-08-18）

- 初回タップ時にBGMとBATTLE START動画を無音再生でウォームアップし、初回BATTLEの再生・描画遅延を抑える。
- BATTLE画面に全ユニットのデバフ名と残りターンを表示する。

## v2.0.68 握り飯の個別ランダム回転（2026-08-18）

- クール=カイの握り飯47発それぞれへ、見た目だけの再現可能なランダム回転を追加する。
- 物理・性能・移動封印は変更しない。

## v2.0.67 クール=カイへ名称変更（2026-08-18）

- キャラクターの表示名を「クールカイ」から「クール=カイ」へ変更する。
- キャラクターキー、素材、必殺技、性能、専用BGMは変更しない。

## v2.0.66 クールカイ必殺後の自己移動封印（2026-08-18）

- 必殺技「Amour 握り飯」の発動後、クールカイ自身へ6ターンの移動封印を付与する。
- 47発の連射、弾の判定・ダメージ・地形破壊、専用BGMの仕様は変更しない。

## v2.0.65 クールカイ火力・接地調整（2026-08-18）

- クールカイの最大HPを66へ変更。
- 握り飯1発のダメージを6へ変更し、見た目3倍・判定半径3・地形破壊は維持。
- バトル中のクールカイ画像を地面アンカーへ31px寄せ、浮きを補正する。

## 直近の変更

| 版・日付 | 内容 | 確認 |
| --- | --- | --- |
| v2.0.58 / 2026-08-17 | ONLINEロビーのBGMをRemixへ更新 | Stage 3 484/484・Android 9/9 |
| v2.0.57 / 2026-08-17 | 公開部屋を一覧から選べるように改善 | CI成功後にmasterへ反映 |

## v2.0.61 クールカイ仕様更新（2026-08-18）

- 必殺技名を `Amour 握り飯`、説明を「手燭の油で作った47個の握り飯を配ってやる。」へ変更。
- 握り飯は固定の大きめ微バラツキで47発、1発2ダメージ、着弾地点を小さく地形破壊する。
- クールカイが参加する対戦では、専用BGM `SIX ÉTERNEL ―愛はひとつじゃない―.mp3` をBATTLE中に固定再生する。
- Regression **406/406**、フル `npm test` も全件成功。

## v2.0.62 ロード中の更新案内（2026-08-18）

- 石壁のロード画面に「更新時は読み込みに時間がかかる場合があります」を表示する。
- タイトル前に画像・UI素材をまとめて準備する仕様を、ユーザーが把握しやすくするための案内。

| v2.0.56 / 2026-08-17 | 演習WIND POWERを戦闘の0〜10表示と統一 | Regression 402/402・Stage 3 481/481 |
| v2.0.55 / 2026-08-17 | 試合の一時状態リセットを統一 | Stage 3 481/481 |

## 次に行う承認済みタスク

各項目は **1PR・1目的** で進める。番号順は実装の目安であり、仕様判断が残るものは着手しない。

1. 不要なリセット重複を整理する（現状の意図を確認してから）。
2. 読込・描画の軽量化: Service Workerの重複入口削除、ヘリコプター画像の同等品質WebP化、地形縁の局所再描画、静止UI/遠景/矩形のキャッシュ、アイドル時30fps、地形キャンバス高さ縮小、DEAD LINEの軽量発光。
3. 通信量改善: 対戦開始時だけ地形基底を配り、以後は差分化する。移動同期を0.25秒・8pxにする。公開部屋の一覧を作成日時順最大24件へ絞る。
4. ランキングの不正行を非表示にできる列を追加する。HMACは使わない。
5. チュートリアル/CPU/キャラクター/オンライン導線の、下記「確定済みゲーム仕様」を実装する。

## 確定済みゲーム仕様

### チュートリアル・CPU・キャラクター

- 初回扱いは `katamon_tutorial_v1` が未完了の端末だけ。既存ユーザーの設定は更新で巻き戻さない。
- 初回だけチュートリアルを目立たせる。チュートリアル②は両者を近づけ、開始時だけ両者が入るカメラ倍率へ引く。保存済みの視点設定は変更しない。
- チュートリアルの達成表示は即時でよいが、次フェーズへの遷移はそのターンの着弾・解決完了後にする。
- フェニーチェの地炎は 1ヒット10、3ヒット。クロムギア3連射は各弾 `damageMul: 0.62`。ゴーロッカのBインパクトは爆風倍率1.9、地形破壊倍率1.5とノックバックは維持する。
- CPU難易度は連勝に応じて連続的に上げる。2連勝から移動、必殺は `min(1, streak / 12)` の確率で段階的に使う。
- ジャンプ性能は全キャラで通常弾と完全共通にする。選択カードにはHP、燃料、必殺技名・説明を表示し、跳躍力は表示しない。
- CPUは足場状況を見てジャンプを使う。
- キャラクター説明文は現行仕様に更新する。

### 連勝・画面表示

- 勝利画面に「記録して連勝を終える」を置く。押すと確認後にランキング記録、保留中の連勝を消去、0連勝から再開してタイトルへ戻る。連勝が終わることを文言で明記する。
- 戦闘中の風表示は可読性を上げ、風力は既存の強弱表現に加えて0〜10で表示する。

### オンライン対戦

- オンライン対戦前にプレイヤー名設定を必須にする。タイトルの空き領域に設定ボタンを置き、名前がない場合はCPU BATTLE/ONLINE BATTLEへ遷移させず案内する。チュートリアルと演習は名前なしで開始できる。
- 「すぐ対戦」「部屋IDを使う」は廃止し、「部屋を作る」「部屋を探す」へ変更する。
- 部屋作成時に部屋名（24字以内）、1vs1/2vs2、公開/鍵付き（部屋IDで入室）を選ぶ。ホスト名と部屋名は公開一覧に表示する。部屋名は開始前だけホストが編集できる。
- 部屋探しは、ホスト名・部屋名・形式・人数・経過時間を表示し、すべて/1vs1/2vs2の手動フィルタと手動更新、部屋ID入室を備える。自動ポーリングはしない。
- 待機ホストには「部屋を閉じてタイトルへ戻る」を置く。60秒で相手手番の案内、90秒で手動退出を強調する。自動発射・自動敗北はしない。
- 対人1vs1で相手が退出したときだけ、端末内戦績へ不戦勝を記録する。ランキングやFirebaseの勝敗には使わない。

## v2.0.39 ボスCPU補正（PR #205予定）

### 何を

- 10連勝ごとのCPUボスだけ、キャラクター本来の最大HPを30%増やし、必殺チャージを開始時から4/4にする。

### なぜ

- 節目のボス戦が、専用闘技場の見た目だけで終わらず、短い一戦でも明確な強敵として機能するようにする。

### やってはいけないこと

- プレイヤー側のHP・チャージ、ボス闘技場、通常戦、CPUキャラの抽選は変更しない。Firebaseルールも変更しない。

### 実測テスト数

- 旧実装では新規Regressionが `HP 130 / 必殺 0` となり失敗することを確認後、修正後はRegression **401/401**、`npm test` **1,191件**（loopback中継数 **38 / 64 / 83 / 61 / 48**）が成功。
- ローカルHTTPの実ブラウザ確認はAndroid Chromium **1件成功**・Mobile WebKit **1件スキップ**（既知のcanvasクラッシュ）。タイトル、CPU開始、演習、チュートリアルでpageerror 0件。

## v2.0.40 Firebase通信の堅牢化（PR #206予定）

### 何を / なぜ

- Firebase直通信を10秒で打ち切る。生存印に失敗した時の確認は部屋全体ではなく `slots` だけを読み、連続3回失敗で案内を表示して停止する。復帰時は再開する。
- 電波不良で通信量と待ち時間が増える逆転現象を防ぎ、利用者に状況を伝えるため。

### やってはいけないこと / 実測テスト数

- Firebaseルール、対戦の権威モデル、通常時20秒の生存印、音声・ランキング通信は変えない。
- 旧実装で新規Stage 3検証が失敗後、Stage 3 **468/468**、`npm test` **1,192件**（loopback中継数 **38 / 64 / 83 / 61 / 48**）成功。ローカルHTTPのAndroid Chromium E2Eは1件成功・pageerror 0件（Mobile WebKit 1件は既知canvasクラッシュでスキップ）。

## v2.0.41 到達不能確認ダイアログの削除（PR #207予定）

### 何を / なぜ

- 呼び出し元がないCanvasの「はい/いいえ」確認、描画、入力優先分岐、リセットを削除する。端末戻るの `deviceBackConfirm` は残す。
- 到達可能性を毎回確認させる死んだ分岐をなくし、入力処理を読みやすく保つため。

### やってはいけないこと / 実測テスト数

- 端末戻る確認、オンライン退出確認、ゲーム仕様は変更しない。
- 旧実装でStage 3新規検証が失敗後、Stage 3 **469/469**、`npm test` **1,193件**（loopback **38 / 64 / 83 / 61 / 48**）成功。

## v2.0.42 APP_SHELL重複入口の削除（PR #208予定）

### 何を / なぜ

- Service WorkerのAPP_SHELLから `./` を削除し、同じ `index.html` の二重初回取得をなくす。
- ナビゲーションのフォールバックは `./index.html` のまま維持するため、オフライン起動体験を変えずに通信を減らす。

### やってはいけないこと / 実測テスト数

- 他のAPP_SHELL資産、Service Workerの更新契約、画面仕様は変えない。
- 旧実装でApp Shell新規検証が失敗後、App Shell **3/3**、`npm test` **1,193件**（loopback **38 / 64 / 83 / 61 / 48**）成功。ローカルHTTPのAndroid Chromium E2Eは1件成功・pageerror 0件（Mobile WebKit 1件は既知canvasクラッシュでスキップ）。

## v2.0.43 DEAD LINE発光の軽量化（PR #209予定）

### 何を / なぜ

- 画面幅いっぱいのDEAD LINEに掛けていた影を、太さの異なる3本の赤い破線へ置き換える。
- 発光した見た目を保ったまま、毎フレームの長い線への高コストなぼかしをなくすため。

### やってはいけないこと / 実測テスト数

- DEAD LINEの位置、点滅、当たり判定、他の描画やゲーム仕様は変えない。
- 旧実装でStage 3新規検証が失敗後、Stage 3 **470/470**、`npm test` **1,194件**（loopback **38 / 64 / 83 / 61 / 48**）成功。ローカルHTTPのAndroid ChromiumでDEAD LINEの発光を目視し、タイトル・CPU開始・演習・チュートリアルE2Eは1件成功・pageerror 0件（Mobile WebKit 1件は既知canvasクラッシュでスキップ）。

## v2.0.44 Canvas矩形キャッシュ（PR #210予定）

### 何を / なぜ

- Canvasの表示矩形をresize時にだけ更新し、ポインタ移動ごとのレイアウト計測をなくす。
- 入力座標の計算式は維持したまま、照準・ドラッグ・ピンチ中の不要な負荷を減らすため。

### やってはいけないこと / 実測テスト数

- 座標変換、タッチ・マウス操作、Canvasの表示サイズ、ゲーム仕様は変えない。
- 旧実装でStage 3新規検証が失敗後、Stage 3 **471/471**、`npm test` **1,195件**（loopback **38 / 64 / 83 / 61 / 48**）成功。ローカルHTTPのAndroid Chromium E2Eは1件成功・pageerror 0件（Mobile WebKit 1件は既知canvasクラッシュでスキップ）。

## v2.0.45 移動同期の通信量削減（PR #211予定）

### 何を / なぜ

- 移動同期を0.12秒・1.5pxから0.25秒・8pxへ調整する。
- 既存の歩行補間を使い、対戦中の見た目を保ったまま位置・燃料の通信回数を減らすため。

### やってはいけないこと / 実測テスト数

- 発射前の最終位置送信、受信側の歩行補間、Firebaseルール、ゲーム操作は変えない。
- 旧実装でStage 3新規検証が失敗後、Stage 3 **471/471**、`npm test` **1,195件**（loopback **38 / 64 / 83 / 61 / 48**）成功。ローカルHTTPのAndroid Chromium E2Eは1件成功・pageerror 0件（Mobile WebKit 1件は既知canvasクラッシュでスキップ）。

## v2.0.56 演習・戦闘の風力尺度統一（PR予定）

### 何を / なぜ

- 演習の `WIND POWER` を0〜10の11段階へ変え、選んだ数値を風力の10分率として使う。カスタムステージも同じ数値で表示する。
- 戦闘中は方向矢印へ数値だけを直接入れ、弱・中・強と `/10` 表記をなくす。

### やってはいけないこと / テスト

- 風の物理、ランダム風、無風、NEXT予報、オンライン同期は変えない。
- 旧実装でRegressionとStage 3が失敗後、`npm test` **1,206件**（Regression **402/402**、Stage 3 **481/481**、loopback中継数 **38 / 64 / 83 / 61 / 48**）とAndroid Chromium E2E **1件成功・pageerror 0件**。

## v2.0.55 一時戦闘状態リセットの統一（PR #221予定）

### 何を / なぜ

- スナップショット適用、再戦、結果からタイトルへの退出が、同じ `resetTransientBattleState()` で弾・演出・入力・メニューを初期化するよう統一した。
- 結果退出だけに残っていた粒子・炎・入力状態を明示的に捨て、将来のリセット漏れを防ぐ。

### やってはいけないこと / テスト

- ボス表示、開始カットイン、順位送信、カメラ設定、地形・通信・ゲームルールは各呼び出し側で従来どおり管理する。
- 旧実装でStage 3新規検証が **480/481** に失敗後、実装後 Stage 3 **481/481**、`npm test` **1,205件**、loopback中継数 **38 / 64 / 83 / 61 / 48** 成功。Android Chromium E2E 1件成功・pageerror 0件（WebKit 1件は既知canvasクラッシュでスキップ）。

## v2.0.54 地形基底の開始時同期（PR #220予定）

### 何を / なぜ

- Firebase対戦の `start` は従来どおり完全な地形基底（segments・bridge・stage設定）を送り、以後の `state` は破壊差分（craters）と対戦状態だけにした。毎ターン不変の地形を再送しないため、通信量と受信側の復元負荷を下げる。
- `state` 受信時は、開始時に受け取ったローカル地形基底へリモートの差分だけを合成する。1vs1・2vs2・空席CPUの席権限を既存どおり維持する。
- `database.rules.json` のsnap必須項目は地形基底を要求していないため、Firebase Console反映は不要。

### やってはいけないこと / テスト

- startまで地形基底を省略しない。観戦途中参加は廃止済みで復活させない。地形の決定論・行動側権威・ターン境界スナップショット設計は変えない。
- 旧実装では「turn-boundary state omits immutable terrain…」契約が失敗することを確認後、Stage 3 **480/480**、Regression **401/401**、`npm test` **1,204件**、loopback **103/103**（中継数 **38 / 64 / 83 / 61 / 48**）成功。ローカルHTTPのAndroid Chromium E2Eは **1件成功・pageerror 0件**、Mobile WebKit 1件は既知canvasクラッシュでスキップ。

## v2.0.53 地形Canvas高さの縮小（PR #219予定）

### 何を / なぜ

- 地形・縁線・橋・闘技場装飾と合成用の6枚を、960固定でなく実際の `TERRAIN_BOTTOM_Y` までに縮める。
- 標準ステージでは操作パネル下の空き約3分の1を6枚ぶん確保しないため、見た目と座標系を変えずメモリと合成領域を減らせる。

### やってはいけないこと / テスト

- 地形の座標、当たり判定、クレーター、地形生成、操作パネル、遠景Canvasは変えない。大型ステージは従来どおり死線924まで確保する。
- 旧実装で新規Stage 3検証が **477/478** に失敗することを確認後、実装後 Stage 3 **478/478**、Regression **401/401**、`npm test` **1,202件**（loopback **38 / 64 / 83 / 61 / 48**）成功。ローカルHTTPのAndroid Chromium E2Eは1件成功・pageerror 0件（Mobile WebKit 1件は既知canvasクラッシュでスキップ）。

## v2.0.52 待機30fps時の固定刻み保持（PR #218予定）

### 何を / なぜ

- 戦闘待機中の30fpsフレームでも、1/30秒ぶんの固定刻みを丸め落とさずに渡す。
- 0.033秒の上限は30fpsの約0.0333秒より短く、待機が続くほど物理時間がわずかに遅れるため。

### やってはいけないこと / テスト

- 30fpsの対象条件、演出・入力中の60fps、固定刻み幅、通信とゲームルールは変えない。
- 旧実装で新規Stage 3検証が **476/477** に失敗することを確認後、実装後 Stage 3 **477/477**、Regression **401/401**、`npm test` **1,201件**（loopback **38 / 64 / 83 / 61 / 48**）成功。ローカルHTTPのAndroid Chromium E2Eは1件成功・pageerror 0件（Mobile WebKit 1件は既知canvasクラッシュでスキップ）。

## v2.0.51 風力0〜10表示（PR #217予定）

### 何を / なぜ

- 戦闘中の現在風の見出しへ、既存の弱・中・強を残したまま実際の強さを0〜10で併記する。
- 同じ「弱」の中にもある差を読み取り、風を学びながら狙えるようにする。

### やってはいけないこと / テスト

- 風の物理、乱数、NEXT予報、無風表示、既存の強弱ラベルとレイアウトを変えない。
- 旧実装で新規Stage 3検証が **475/476** に失敗することを確認後、実装後 Stage 3 **476/476**、Regression **401/401**、`npm test` **1,200件**（loopback **38 / 64 / 83 / 61 / 48**）成功。ローカルHTTPのAndroid Chromium E2Eは1件成功・pageerror 0件（Mobile WebKit 1件は既知canvasクラッシュでスキップ）。

## v2.0.50 戦闘待機中の30fps化（PR #216）

### 何を / なぜ

- 弾・入力・カットイン・粒子・カメラ移動がない戦闘待機中だけ、描画と更新を30fpsへ抑える。
- 風景の雲とDEAD LINEの動きは残し、操作・発射・演出が始まれば即座に通常の更新頻度へ戻す。

### やってはいけないこと / テスト

- 固定刻み物理、通信、操作入力、弾・必殺・演出中の60fpsを変えない。
- 旧実装で新規Stage 3検証が **474/475** に失敗することを確認後、実装後 **475/475**、Regression **401/401**、`npm test` **1,199件**（loopback **38 / 64 / 83 / 61 / 48**）成功。ローカルHTTPのAndroid Chromium E2Eは1件成功・pageerror 0件（Mobile WebKit 1件は既知canvasクラッシュでスキップ）。

## v2.0.49 バルコプター画像のWebP化（PR #215）

### 何を / なぜ

- 必殺技時だけ遅延読込するバルコプター専用画像をPNGからWebPへ切り替えた。PNG 4,585,353Bに対してWebP 539,346Bで、画質を保って約88%小さくした。

### やってはいけないこと / テスト

- PNGは削除しない。バルコプターの表示サイズ、遅延読込、必殺技の挙動、他キャラ画像は変えない。
- 旧実装でWebP専用素材のStage 3検証が **473/474** に失敗することを確認後、実装後 **474/474**、`npm test` **1,198件**（loopback **38 / 64 / 83 / 61 / 48**）成功。ローカルHTTPのAndroid Chromium E2Eは1件成功・pageerror 0件（Mobile WebKit 1件は既知canvasクラッシュでスキップ）。

## v2.0.48 地形縁線更新の集約（PR #214）

### 何を / なぜ

- 固定刻み物理中の花火・死神・サソリ軌跡とヘリ弾の地形削りは、縁線更新を予約し、描画フレーム末尾で最大1回だけ再構築する。
- チュートリアル足場の連続掘削も、グリッド更新は即時のまま縁線だけ最後に1回へまとめる。

### やってはいけないこと / テスト

- 地形グリッド、当たり判定、クレーター履歴、固定刻み、雲・物理・通信は変えない。
- 旧実装で新規Stage 3検証が **473/474** に失敗することを確認後、実装後 **474/474**、Regression **401/401**、`npm test` **1,198件**（loopback **38 / 64 / 83 / 61 / 48**）成功。ローカルHTTPのAndroid Chromium E2Eは1件成功・pageerror 0件（Mobile WebKit 1件は既知canvasクラッシュでスキップ）。

## v2.0.47 遠景の丘のキャッシュ（PR #213）

### 何を / なぜ

- 試合中に形が変わらない遠景の丘2層を、試合ごとに必要な時だけオフスクリーンCanvasへ描き、通常フレームは貼り付けだけにした。
- 丘の層ごとのカメラ追従率は従来どおり別なのでCanvasも分け、雲は動く演出のため従来どおり毎フレーム描く。

### やってはいけないこと / テスト

- 雲を丘のキャッシュへ混ぜない。専用ステージ背景がある時の遠景省略も変えない。
- 旧実装で新規Stage 3検証が **472/473** に失敗することを確認後、実装後 **473/473**、`npm test` **1,197件**（loopback **38 / 64 / 83 / 61 / 48**）成功。ローカルHTTPのAndroid Chromium E2Eは1件成功・pageerror 0件（Mobile WebKit 1件は既知canvasクラッシュでスキップ）。

## 保留・ユーザー判断待ち

- `content-studio-mvp` の10コミットは削除せず保持する。内容を独立して再評価してからPR化する。
- 音声ノイズの事前計算（PERF-5）と、必殺技定義の大規模リファクタリング（ARCH-4）は後回し。
- SSE再接続の `limitToLast` は、差分スナップショット化後に現物の購読設計を確認して判断する。

## 絶対に守ること

- ローカルの古いルートworktreeを正本扱いせず、作業前に `git fetch origin` し、`origin/master` から新しい作業ブランチ/worktreeを作る。
- 既存worktree、`.codex-remote-attachments`、`catamon_vertical_pv`、`shared` は触らない。`shared/` は追跡済み資産なので無視対象にも入れない。
- `database.rules.json` を変えるPRは、公開前にFirebase Console反映をメロニキへ依頼する。
- 新テストは、壊した旧実装で実際に失敗することを確認してから追加する。毎回テスト件数を報告する。
- 見た目の変更は `file://` を使わず、ローカルHTTPと実ブラウザで確認する。環境上できない場合は理由をこのファイルとPRへ残し、他の自動テストを完走する。
- 描画だけの変更でloopback中継数が基準値 38 / 64 / 83 / 61 / 48 から動いたら、理由を説明できるまで進めない。
- 行動側権威＋ターン境界スナップショット、地形の現物配布、既存のコメントが説明する意図は変えない。

## 現在のテスト基準

- 直近フル実行の内訳: 文書上限 1、Seat 40、Regression 401、Result 93、Loopback 103、Stage3 467、Lobby 7、Back 14、Ranking 5、未定義走査 2、App Shell整合 3、キャッシュ契約 2、Stage 53（合計1,191）。ボスCPU開始条件の回帰1件を追加。以後は増減理由と実測件数を記録する。
- loopbacktest中継数の基準: **38 / 64 / 83 / 61 / 48**。
- 本体E2EはAndroid Chromiumでタイトル・CPU開始・演習・チュートリアルを通し、pageerror 0件を確認する。現行ローカル環境のMobile WebKitは本体canvas読込前にブラウザがクラッシュするため、この1件はスキップする。Stage Studioの既存WebKit E2Eは維持する。

## 2026-08-17 安全網追加（PR #201予定）

### 何を

- 席ハーネスが、インラインゲームscriptより前の外部scriptをブラウザと同じ順で読むようにした。
- `APP_SHELL` と `index.html` の外部script登録、未定義の大文字識別子、ゲーム本体の主要導線でのpageerrorをCIで検査する。

### なぜ

- 将来の分割時のscript読込漏れ、Service Workerへの登録漏れ、画面遷移時の例外を公開前に止めるため。

### やってはいけないこと

- `shared/` を移動・削除・無視対象化しない。ハーネスの欠落確認はテスト専用の安全な模擬で行う。
- ゲーム本体の画面遷移・通信・物理仕様をこの目的で変更しない。

### 実測テスト数・確認

- 外部script欠落の模擬では席テストが exit 1 で失敗することを確認後、通常のSeat 40/40を確認。
- `npm test` は **1,187件** 成功。文書は 8,710B / 30,720B。
- 新しい本体E2EはローカルHTTP＋Android Chromiumで **1件成功**、タイトル・CPU開始・演習・チュートリアルのpageerror 0件。
- Mobile WebKitは本体canvas読込前にブラウザがクラッシュするため当該1件をスキップ。既存E2E全体はこの端末で4分を超えて完走ログを取得できなかったため、PRのGitHub CIで再確認する。

## v2.0.36 キャッシュ版管理の一本化（PR #202）

### 何を

- `index.html` と `sw.js` のアセットURLから `?v=` を除去し、BUILD_ID/CACHE_VERSIONを `v2.0.36-cache-version-contract` に一致させた。
- Service Workerは検索条件を無視せず、新しいCACHE_VERSIONのAPP_SHELLを `cache: 'reload'` で再取得する。

### なぜ

- `ignoreSearch: true` により個別の `?v=`更新がService Worker配下で効かない矛盾をなくし、更新手順を版番号の更新だけへ統一するため。

### やってはいけないこと

- ロゴ画像・ロゴ動画・タイトルBGM・全ステージ背景の初回読込方針を変えない。
- BUILD_ID と CACHE_VERSIONを別の版にしない。

### 実測テスト数・確認

- 修正前の契約テストは `ignoreSearch: true` を検出して失敗。
- 修正後は契約テスト 2/2、Stage 3 467/467、フル `npm test` 1,189件成功。ローカルHTTPのAndroid Chromium本体E2E 1件成功（Mobile WebKitは既知の環境クラッシュにより1件スキップ）。

## v2.0.37 途中参加スナップショットの廃止（PR #203予定）

### 何を

- Firebase各roundの `latestSnapshot` ノード、書込み、読み出しを削除した。
- ロビー中から観戦席にいた人へは、従来どおり開始パケットで盤面を渡す。

### なぜ

- 対戦途中の参加受付はそもそも閉じており、誰にも読まれない全盤面を毎ターン保存する通信をなくすため。

### やってはいけないこと

- 対戦者の開始・ターン境界スナップショット、観戦席がロビーから開始時に観戦する動線は変えない。
- `database.rules.json` をFirebase Consoleへ反映せずに公開しない。

### 実測テスト数・確認

- 修正前のStage 3は、廃止検証2件が `saveSnapshot` / `latestSnapshot` を検出して失敗。
- 修正後はStage 3 467/467、フル `npm test` 1,189件成功。ローカルHTTPのAndroid Chromium本体E2E 1件成功（Mobile WebKitは既知の環境クラッシュにより1件スキップ）。CIとFirebase Console反映はこのあと確認する。

## v2.0.38 必殺弾道の通常弾統一（PR #204予定）

### 何を

- フェニーチェ必殺とクロムギア必殺の中央弾を、通常弾と同じ初速・風・重力へ統一した。
- クロムギアの左右±5°三連射と、フェニーチェの着弾後の地炎は残した。

### なぜ

- 手動の狙い線、CPUの逆算、実際の発射物理が同じ軌道を示すようにするため。

### やってはいけないこと

- 今回は必殺ダメージ、地炎回数、三連射の角度を変えない。
- CPU難易度やボス補正を同じPRへ混ぜない。

### 実測テスト数・確認

- 修正前のRegressionは新規2検証が失敗し、クロムギア中央弾の初速150・風0.8を検出。
- 修正後はRegression 400/400、フル `npm test` 1,190件成功。ローカルHTTPのAndroid Chromium本体E2E 1件成功（Mobile WebKitは既知の環境クラッシュにより1件スキップ）。CIはこのあと確認する。

## 更新ルール

- 各PRで「何を / なぜ / やってはいけないこと / 実測テスト数」を、該当する未完了タスクまたは直近変更へ簡潔に追記する。
- `CURRENT_WORK_STATE.md` が **30KB** を超える変更はCIで失敗する。完了済みの詳細は `docs/CHANGELOG.md` へ移す。
# v2.0.59

- 教程は行動解決後に遷移。着弾前の誤達成を防ぐ。戦闘不変。旧実装でRegression失敗→npm test 1,613件。実ブラウザは開始まで確認、E2E依存取得は環境停止。
