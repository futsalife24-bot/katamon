# カタモン 引き継ぎ(2026-07-27・ルーム専用ページ化まで完了)

**この文書が最新の入口。** 前段は `HANDOFF_2026-07-27_CODEX_TO_CLAUDE.md`(Codex→Claude)、
通信仕様の詳細は `HANDOFF_2026-07-26.md`。設計の根拠は `DESIGN_2026-07-27_ROOM_SCENE.md`。

## 0. 現在地

- HEAD `770df87`、push済み。`BUILD_ID` / `CACHE_VERSION` ともに **v58**
- 公開版 https://futsalife24-bot.github.io/katamon/?v=58
- テストは全部通る(stage3 **184**/184、seat 19×2、regression 20×2、result 26、loopback 93)
- Firebaseルールは **2026-07-27 に反映済み**(§3の修正を含む)
- ⚠️ **作業ツリーに `tests/README.md` / `tests/loopbacktest.js` / `tests/peer.js` の未コミット差分がある。**
  これは別セッションで走らせた loopback フレーク修正タスクの成果物で、**私は一切触っていない**。
  中身を確認してからコミットするか、担当セッションに委ねること

## 1. ユーザーとの取り決め(変更なし)

1. 通常の `git push origin master` は確認不要
2. force push・履歴書き換えは確認する
3. **`database.rules.json` を変えたらユーザーにConsole反映してもらってから push**
4. 版を上げる時は `BUILD_ID` と `sw.js` の `CACHE_VERSION` を同時に
5. URLは必ず `?v=NN` 付きで渡す。**添付ファイルは渡せない**(リモート画面では受け取れない)
6. 非エンジニア向けに、原因・変更・実機で見てほしい点を平易な日本語で
7. **実装は軽量モデル(Sonnet)のエージェントに下ろす。** Fable 5 は設計・企画の分岐点だけ

## 2. このセッションでやったこと

### 2-1. ルーム専用ページ化 R1〜R4(完了)

Fable 5 が設計(`DESIGN_2026-07-27_ROOM_SCENE.md`)、実装はSonnetエージェント、レビューとコミットは私。

| 段階 | build | 内容 |
|---|---|---|
| R1 `d0fa64b` | v54 | `#onlineLobby` をモーダル→**全画面DOMスクリーン**へ。ヘッダ/本文/フッタの3段。主導線は最下段の全幅ボタン |
| R2 `61d9e86` | v56 | **BGMディレクター `syncBgm()`**。約10箇所の play/stop 直呼びを廃止し一元化 |
| R3 `fe361ea` | v57 | **ルーム専用BGM**(`assets/room-bgm.mp3`)をディレクターの3曲目として追加 |
| R4 `770df87` | v58 | **席ボードの行カード化**と**退出のローカル優先化**、エラー時の赤帯 |

**設計上の最重要判断(§0):専用ページはCanvasシーンでも別URLでもなく全画面DOM。**
理由はロビーがDOMである理由(IME・クリップボード・共有)が専用ページ化しても消えないから。
`gamePhase` は増やしていない。ルーム表示中も水面下は `title` のまま。

**BGMの決定関数はここだけ:**
```
desiredBgm() = hidden or ミュート → 'none'
             | ルーム画面が開いている && !autoStartNextRound → 'room'
             | gamePhase === 'battle' → 'stage'
             | それ以外 → 'title'
```
- **ルーム判定が battle 判定より先**でなければならない(決着後「ロビーへ戻る」で開くロビーは
  `gamePhase` が battle のままのため)。テストで順序を固定してある
- **双方「このまま再戦」の間はルーム曲を挟まない**(数秒で次戦に入るため。`autoStartNextRound` で除外)
- **触っていない例外3つ**:初回タップの音声アンロック経路、タイトル曲のギャップ付きループ、戦闘中のダック
- テストが `playStageBgm(` 等の**出現回数を数えて固定**している。遷移箇所に直接 play を書き足すと落ちる

### 2-2. タイトル・キャラ選択のUI刷新(`1d03722` / v55)

- **`ONLINE 1V1` → `ONLINE BATTLE`**(4席あり将来2vs2も入るため、人数を名前に埋めない)。
  タイトル・ルーム見出し・共有文面の4箇所すべて
- タイトルメニュー:紋章バッジ廃止・ラベル中央寄せ・共通トークンへ・
  `SELECT A BATTLE MODE` をボタン群の**一番上**へ
- 出撃ギア:**ホイールと連動して回転**(カード1枚=歯1枚分なので静止時のシルエットが必ず揃う)。
  文字と中心ハブは正立のまま。ネオン配色を黒鉄+真鍮へ
- キャラ選択カード:紺ネオン地→黒鉄プレート+真鍮枠。**キャラ固有色は役割チップ・
  ステータス・発光のアクセントとして残す**(全部真鍮にすると全カードが同じ顔になる)
- **到達不能な旧カード描画161行を削除**(存在しない `drawFocusedSelectCard` を呼んでいた)

### 2-3. ⚠️ 簡単対戦の永久詰み欠陥を修正(`bafb7af`)

**実機で「すぐ対戦するを押しても利用中と言われる」報告 → 本番DBを診断して確定。**

原因は三段構えだった:
1. 部屋の削除はホスト本人にしか許されていない
2. ホストの匿名認証は使い捨てで、ブラウザを閉じた時点で削除できる人が消える
3. 新規作成は「データが存在しない時だけ」許可 → 残骸があると作れない

合言葉部屋は毎回コードが違うので実害が出ないが、**固定コードの簡単対戦(`KATAMN22`)は
一度放置されると永久に使用不能**になる。

**修正**:期限切れ(`expiresAt <= now`)の部屋は**誰でも上書き作成・削除できる**。
`hostUid` / `createdAt` の不変検証も期限切れ時だけ緩めた(緩めないと上書きがそこで弾かれる)。
有効な部屋の保護は従来どおり。**本番で 9/9 検証**(`scratchpad/repro_expired_takeover.js`)し、
`KATAMN22` の残骸を削除して回復を確認済み。**クライアント変更は不要**で旧版端末でも自己回復する。

**残る穴**:ゴーストが**有効期限内**(最大2時間)の部屋を握るケースは救えない。
生きているロビーと区別が付かないため。これはStage 4(生存確認)の領分。

## 3. 実機確認の状況

### 確認済み(ユーザー)
- v47/v52/v53 の3点(BGM二重再生なし・視点スライダー・最新版を取得)
- v54 の全画面ルーム画面(入口・合言葉モード)
- v56 のBGM(全遷移・バックグラウンド復帰とも異常なし)

### 未確認
- **対戦の通し(入室→対戦→結果→再戦/ロビー復帰)。サブ機が空いた時にやる約束。**
  R1でレイアウトを、R3でBGMを、R4で席ボードと退出を変えているので、ここを通すのが最優先
- v55 のタイトル・キャラ選択の見た目、v57 のルーム曲、v58 の席ボード・退出2択
- 簡単対戦が回復しているか(§2-3の修正後)
- 画面ロック・回線切替・切断後の再入室(Stage 4の領分)
- 観戦者を含めた完走

## 4. 次にやること(優先順)

1. **上記の通し確認**。ここで出た不具合が最優先
2. **Stage 4:切断・画面ロック・再入室・ロビー中の心拍。**
   R4で「応答なし」表示を見送った理由がここにある:
   `updateFirebasePeerLiveness()` は `phase !== 'playing'` で早期returnし、
   **ロビー中は生存情報がそもそも更新されない**。presence も入室時の1回だけ。
   ロビーへの心拍を新設するのがStage 4の入口になる
3. Codex引き継ぎ書 §6 のP2以降(タイトルの更新履歴表示、CPU中断データの再開案内、
   音量OFFアイコンの統一、闘技場の壁の描画、マップ・キャラの新規コンテンツ)
4. GASランキングの `character` 列の再デプロイ確認(優先度低)

## 5. 作業のやり方(効いたこと・しくじったこと)

### 効いたこと
- **実機ログ/本番DBを見てから直す。** 簡単対戦の件は、推測せず本番へ実際に投げて
  「4席とも401・新規作成も401」を確認したから一発で原因が確定した
- **エージェントの報告を鵜呑みにしない。** 今回3件拾った:
  - R1で「部屋を作る/入る」ボタンの装飾が消えていた(エージェントの検証は
    「見える/見えない」だけで見た目の欠落を素通り)
  - R4で「無関係な既知失敗」と報告された1件が、**実は私のルール修正で壊れた検査**だった
  - R3で観戦者の結果ロビーにBGM同期が無く、不確定状態が残っていた
- **段階を切って、一番危ない統合を「何も変わらないはずの回」として単独で出す**(R2)。
  壊れたら原因が一意に決まる

### しくじったこと
- **ルール修正(`bafb7af`)をテストを回さずにpushした。** 検査1件が落ちたまま残り、
  次のエージェントに「無関係な既知失敗」と誤認された。**ルール変更もテストを回してからpush**
- Fable 5 をクレジット不足のまま起動しようとした(先に残高を確認すべきだった)

### 環境の罠
- **`core.autocrlf=true` で作業ツリーがCRLFになる。** 改行を含む文字列を `includes()` で
  比べているテストが落ちるので、`stage3test.js` は読み込み時にLFへ正規化してある(`f779fe5`)
- **Bashのヒアドキュメントはバックスラッシュを壊す。** 正規表現を含むパッチは
  Writeツールで .py を書いてから実行する
- **`computer{screenshot}` が不調。** `javascript_tool` で `getComputedStyle` /
  `getBoundingClientRect` を読んで検証する方が確実
- プレビューサーバのルートは**カタモンフォルダ**。`/カタモン/index.html` は空ページになる

## 6. 直近コミット

```text
770df87 R4: 席ボードの行カード化と退出のローカル優先化      v58
bafb7af 期限切れの部屋を誰でも上書き作成・削除できるように   (rules)
fe361ea R3: ルーム専用BGMを追加                              v57
61d9e86 R2: BGMをディレクター(syncBgm)へ一元化              v56
1d03722 タイトルとキャラ選択のUIを刷新                       v55
d0fa64b R1: オンラインロビーを全画面のルーム画面レイアウトへ v54
7bf6382 ルーム専用BGMの素材を追加
3ec46fc 対戦ルーム専用ページ化+BGMの設計文書を追加
8ae0ffd 引き継ぎ書:v47/v52/v53の実機確認済みを反映
f779fe5 テストがチェックアウトの改行コードで落ちないようにする
```

## 7. 新セッションへの最初の指示(コピペ用)

> `HANDOFF_2026-07-27_ROOM_SCENE.md` を全文読んでから作業を始めてください。
> まず `git status` で未コミットのテスト差分(別セッションのフレーク修正)を確認し、
> 内容を見て問題なければコミットしてください。
> その後はユーザーの実機確認待ちです。§3の未確認項目のうち
> 「対戦の通し」で不具合が出たら、それを最優先で直してください。
