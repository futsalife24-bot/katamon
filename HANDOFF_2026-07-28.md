# カタモン 引き継ぎ(2026-07-28・Stage 4到達まで)

**この文書が最新の入口。** 前段は `HANDOFF_2026-07-27_ROOM_SCENE.md`(ルーム専用ページ化)、
その前は `HANDOFF_2026-07-27_CODEX_TO_CLAUDE.md`、通信仕様の詳細は `HANDOFF_2026-07-26.md`。

## 0. 現在地

- HEAD `3a8f2b1`、push済み。`BUILD_ID` / `CACHE_VERSION` ともに **v69**
- 公開版 https://futsalife24-bot.github.io/katamon/?v=69
- 作業ツリーはクリーン
- テストは全部通る:
  **lobbysim 7 / stage3 207 / seat 20×2 / regression 23×2 / loopback 93 / result 26**
- **Firebaseルールは 2026-07-28 に反映済み**(`expiresAt` に `.write` を1つ追加。§2-2)
- ⚠️ **別のセッションが同じリポジトリを触っている。** `92983a9`(v64)はこのセッションの
  成果物ではない。**着手前に必ず `git log` と `git status` を見ること**

## 1. ユーザーとの取り決め

1. 通常の `git push origin master` は確認不要
2. force push・履歴書き換えは確認する
3. **`database.rules.json` を変えたらユーザーにConsole反映してもらってから push**
4. 版を上げる時は `BUILD_ID` と `sw.js` の `CACHE_VERSION` を同時に
5. URLは必ず `?v=NN` 付きで渡す。**添付ファイルは渡せない**
6. 非エンジニア向けに、原因・変更・実機で見てほしい点を平易な日本語で
7. **実装前に方針の承認を取る。** 提案と実装を分ける。今回は毎回これで回した
8. 前回の引き継ぎ書にあった「実装は軽量モデルへ下ろす」は、今回は**使っていない**。
   判断の分かれる小さな変更ばかりで、下ろす手間の方が大きかった

## 2. このセッションでやったこと

### 2-1. Stage 4:放置された部屋が塞がる問題(**達成**)

**S4-1 `e9912d4` (v59) ロビーの心拍。**
`updateFirebasePeerLiveness()` は `phase !== 'playing'` で早期returnし、`presence` は入室時の
1回だけだったため、**ロビー中は生存情報が完全に無音**だった。ロビー専用の心拍を
**別系統**で新設(15秒ごとに `ping`、45秒途切れたら席ボードに「応答なし」)。
**対戦中の判定(10s/35s・切断あり)と統合してはいけない。** ロビー側は表示だけで切断しない。
時計はロビー滞在中の可視時間だけを積む(対戦中は止める。長い試合から戻った直後に
全員が応答なしに見えるのを防ぐため)。**実機で「応答なし」表示を確認済み。**

**S4-2 `d9297c8` (v61) 部屋のリース。** ここが本丸。
§2-3(前回)で直したのは「期限切れの部屋」で、**まだ期限内の部屋をホストのゴーストが
握るケースは救えていなかった**。固定コードの簡単対戦(`KATAMN22`)はこれで最大2時間詰む。
部屋の期限を **2時間→10分**に縮め、ホストが60秒ごとに前へ延ばす。ホストが消えれば
10分で期限切れになり、**既存の「期限切れは誰でも上書き」ルールがそのまま発火する**。
乗っ取りの仕組みを新設しないのがこの設計の要点。

ルール変更は `expiresAt` に `.write` を1つ足しただけ:
> ホスト本人が、まだ生きている部屋の期限を、前へ動かす時だけ書き換えてよい

- `data.exists()` … 新規作成の経路には触らせない
- `expiresAt > now` … 期限切れの部屋を延命させない(§2-3の乗っ取りを横から無効化させない)
- `newData.val() > data.val()` … 前へしか動かせない
- **上限(2時間)は据え置いた。** ここを縮めると**旧版を掴んだ端末が部屋を作れなくなる**

**本番で8/8検証済み**(`scratchpad/verify_room_lease.js`。scratchpadは消える。必要なら
コミットログ `d9297c8` の本文に検証項目が残してある)。

**`9d3b1b1` (v62) で終点の後片付けも入れた。** 通信エラー・版不一致・相手の退出で
タイトルへ抜ける経路が部屋を置き去りにしていた。`round.status` が `playing` のまま残ると
席の確保は `status==='lobby'` を条件にしているので**誰も入れない**。実機で実際に詰まった。
ホストは部屋を、ゲストは自分の席を返す。**結果画面からは呼ばない**(再戦に部屋が要る)。
これで**リースは保険に下がり、通常は即座に空く**。

**実機で簡単対戦に入れることを確認済み。**

### 2-2. ダメージ ── 入れて、壊して、戻した(**要点**)

**`0f7eb9e` (v63) で3段の固定値にしたのは失敗だった。`0ca72cb` (v66) で戻した。**

狙いは「オンラインで同期のたびにHPの小修正が見える」の解消。帯ごとの固定値にすれば
多少ズレても両端末で同じ値になる、と考えた。**これが公平性を壊した。**

直撃の境界は20px、キャラ中心から足元までは16px。**足元撃ちは距離16〜25pxに散らばって
ちょうど境界をまたぐ。** 固定値だとそこが**10ダメージの崖**になり、立ち位置と地面の傾きで
どちら側に落ちるかがほぼ決まるため、**片方だけ毎回45・もう片方は毎回35**になった。
傾斜なら同じ状況で45と42程度に収まる。実機の恐竜同士の対戦で実測して発覚。

**教訓:連続量を離散化すると、境界が「よく起きる値」の上に乗った時に不公平が生まれる。**
検査も「帯の中で一定」ではなく「**隣り合う距離で崖が無い(差3以下)**」に書き換えてある。

**`76fbcc0` (v67) が元の要望への正解。** 受け側も弾道を自前で再現して見積もりを
ポップさせ、直後に届く権威ある値でHPだけ上書きされるので**数字が後から動いていた**
(実機で10動いた)。**相手の弾による被弾ではポップを出さず、権威ある値が届いた時点で
差分を出す。** 計算式には触らないのでバランスは動かない。

**固定ステップ案は測って捨てた。** 60fpsと120fpsの着弾差は標準的な山なりで0.3px、
低く速い弾で約9px。9pxは傾斜で6ダメージ相当にしかならず、報告の10には届かない。
**弾の飛び方が変わる副作用のわりに効果が中途半端。**

**未解決:** HPバーそのものは一瞬だけ見積もりで動いてから正しい値に落ち着く。
数字は動かなくなったがバーの微修正は残る。消すには受け側でHPを一切動かさずに
待つ必要があり、決着判定に絡む。

### 2-3. コミット・公開の順序バグ(**この経路は無検査だった**)

**`3a8f2b1` (v69)。** 実機で「キャラクター確認が一致しません」が出て両者が動けなくなった。

`acceptPeerCommit` は「まだ公開前なら新しいもので上書きしてよい」だけで判断していたため、
**古いコミットが後から届くと最新を上書きし**、続いて届く公開が一致しない。
**相手が準備完了を押し直した時にだけ**コミットが2つ飛ぶので、1回で決めている限り
表に出なかった。送信時刻(`sentAt`)で古いものを落とすようにした。比較するのは同じ送信者の
コミット同士なので端末間の時計のズレは関係なく、**落とす方向にしか効かないので
なりすましの余地は増えない**。

**なぜすり抜けたか: `loopbacktest.js` はv2のプロトコルで動き、キャラのコミット・公開は
v3ロビーの機能なので一度も通っていなかった。** `tests/lobbysimtest.js` を新設して埋めた。
ゲームは動かさず本体の判定関数だけを呼び、**配送順を変えて**結果を見る(7項目)。
**「通ってはいけない」検査(公開より先のコミット、公開後の差し替え)が入っているので、
不正防止を緩めると落ちる。**

**`697bc09` (v68) で行き止まりも塞いだ。** 検証で止まると `phase` は `revealing` のままで、
退出は `lobby` 以外を一律で断っていたため**退出も再戦もできなかった**。
対戦中以外は通し、**エラーで止まっている時は `phase` に関わらず必ず通す**。

### 2-4. 表示まわり

- **`9c93dc1` (v60)** 対戦中の切断でも「オンライン対戦を**開始**できません」と出ていた。
  `matchStarted` で「対戦を中断しました」と出し分け。抜け方の案内(「画面をタップして
  タイトルへ」)も追加。実はタップで戻れるのに案内が無く、詰まったように見えていた。
  席が応答なしになった時に状態表示も差し替え
- **`9d3b1b1` (v62)** 出撃ギアが逆回転していた(v55で符号を逆に置いていた)。歯も一回り大きく
- **`0f7eb9e` (v63)** 名前カードを**キャラが立っている側**へ。自分のカードを必ず左に置いて
  いたため、ゲスト(e1)は「名前が左・自キャラは右」を見ていた。判定は位置ではなく**席**で
  行う(試合中にすれ違ってもカードが飛ばないように)。陣営色は置いた側ではなく**持ち主**で引く
- **`2bbc107` (v65)** 闘技場でCPUが自分から奈落へ歩き込んで自滅していた。
  `limitMoveByClimb` が止めるのは急な**上り**だけで、下りと足場の切れ目は素通りしていた。
  `cpuStepIsSafe()` を追加。**プレイヤーには適用しない**(自分の意思で落ちるのは操作の自由)
- **`92983a9` (v64) は別セッションの成果物。** ギアの矢印削除と性能メーターの群の間隔

### 2-5. テストの決定化

**`a9eb893`。** 前セッションから引き継いだ未コミット差分。`peer.js` が `Math.random` を
役割別の固定シードPRNGに差し替える。フレークの正体は「ホストの初弾でゲストが場外死→
ゲストが一度も撃たないまま正常決着」。seed 1/7/42 のいずれでも93/93を確認してからコミットした。

## 3. 実機確認の状況

### 確認済み(ユーザー)
- v47/v52/v53、v54の全画面ルーム画面、v56のBGM(前セッションぶん)
- **v59 ロビーの「応答なし」表示**(スクショで確認)
- **簡単対戦に入れること**(S4-2/v62の効果)
- **v62 出撃ギアの回転向き**
- **v63 ダメージが不公平になっていること**(実測で報告 → v66で戻した)
- **v69 の直前の症状**(キャラ確認が一致しない・退出できない)

### 未確認 ← **ここから始める**
- **v69 押し直してからの対戦開始。** わざと準備完了を2〜3回押し直してから開始するのが
  一番効く確認
- **v65 闘技場でCPUが落ちなくなったか。** 逆に棚の端で固まっていないかも見る
- **v67 撃たれた側の数字が後から動かないか**
- **v66 ダメージが公平に戻ったか**(恐竜同士の足元撃ち)
- **v63 ゲスト側の名前カードの左右と色**(自分のカードが青で、自キャラと同じ側か)
- v64 メーターの間隔と矢印の削除
- 対戦の通し(入室→対戦→結果→再戦)全体
- 画面ロック・回線切替・切断後の再入室(Stage 4-3の領分)

## 4. 次にやること(優先順)

1. **上記の実機確認。** ここで出た不具合が最優先
2. **メモ②「対戦部屋内での戦績表示」。** 着手前に**戦績をどこに持つか**を決める
   (端末内だけ/部屋に持たせる/ランキングと繋ぐ)。ここを決めずに作ると作り直しになる
3. **メモ⑥「ランキングを同一プレイヤー3件まで」。** GASのデプロイが要るので、
   **前回から残っている「`character` 列の再デプロイ確認」と一緒にやる**
4. **HPバーの微修正**(§2-2の未解決)
5. **席のリース(A案)。** ゲスト・観戦席のゴースト。部屋が10分で切れるようになったので
   以前より痛みは小さい
6. **Stage 4-3:切断・画面ロック・再入室**
7. Codex引き継ぎ書§6のP2以降(タイトルの更新履歴表示、CPU中断データの再開案内、
   音量OFFアイコンの統一、闘技場の壁の描画、マップ・キャラの新規コンテンツ)

## 5. 作業のやり方(効いたこと・しくじったこと)

### 効いたこと
- **推測で直さず、本番と実機を見る。** 簡単対戦が「利用中」のままだった件は、本番へ
  実際に投げて「3席とも401・新規作成も401」を確認したから、**席の空きではなく
  `round.status` が固まっている**と特定できた
- **再現してから直す。** コミットの順序バグは、配送順を変えるシミュレーションで
  再現させてから直した。**再現がそのまま回帰テストになった**
- **前提そのものを検査に入れる。** 「CPUが奈落へ踏み込まない」だけだと、地形が変わって
  中央が奈落でなくなった時に素通りで通る。「中央が本当に死線へ届く奈落であること」を
  先に確かめている
- **段階を切る。** S4-1(表示だけ・ルール無変更)を先に出し、S4-2(ルール変更)を後に回した。
  ルールを触った時に不具合が出れば原因が一意に決まる

### しくじったこと
- **ダメージの帯固定(v63)。** 連続量を離散化する時、境界が「よく起きる値」の上に
  乗らないかを確認しなかった。**バランスに触る変更は、代表的な場面の数値を並べてから出す**
- **`update()` の中で `return` を書いた。** CPUの移動を打ち切るつもりが、そのフレームの
  残り全部(弾の更新・タイマー)を飛ばすところだった。書いた直後に気づいて分岐に直した
- **キャッシュの版を疑わなかった。** ユーザーのスクショが build v55 だったのに、
  一度は最新版として読んだ。**スクショの右下の build 番号を必ず見る**
- **コミットメッセージにPowerShellのヒアストリング記法をBashで使った。** メッセージの
  先頭に `@` が混入した(未pushだったのでamendで直した)

### 環境の罠
- **`core.autocrlf=true` で作業ツリーがCRLFになる。** Pythonで文字列置換する時、
  改行を含むマーカーは一致しない。**アンカーは1行に収めるか、Editツールを使う**
- **Bashのヒアドキュメントはバックスラッシュを壊す。** 正規表現を含むパッチは
  Writeツールで .py を書いてから実行する
- **`computer{screenshot}` は使えない**(「Browser paneが表示されていない」で失敗する)。
  DOMなら `javascript_tool` で `getComputedStyle` を読む。**Canvasの描画は検証できないので、
  見た目の変更は必ず「実機で目視してほしい」と伝える**
- プレビューサーバのルートは**カタモンフォルダ**。ポート8944は別セッションが使っている
  ことがある。その場合は `preview_start` に `url` を渡して既存のサーバへ繋ぐ

## 6. 直近コミット

```text
3a8f2b1 v69: 押し直しの古いコミットが最新を上書きしないように
697bc09 v68: キャラ確認で止まった時に退出できるように
76fbcc0 v67: 被弾の数字を、届いた権威ある値だけで出す
0ca72cb v66: ダメージを傾斜に戻す(v63の帯固定は公平性を壊した)
2bbc107 v65: 闘技場でCPUが自分から奈落へ歩き込まないように
92983a9 v64: 出撃ギアの矢印を削除、性能メーターの群を分ける  ← 別セッション
0f7eb9e v63: ダメージを3段の固定値に、名前カードをキャラと同じ側へ
9d3b1b1 v62: 出撃ギアの回転向きと歯の大きさ、終点で部屋を返すように
d9297c8 S4-2: 部屋を短い期限にして、ホストが生きている間だけ延ばす (v61)
9c93dc1 v60: 切断まわりの表示を実機報告どおりに直す
e9912d4 S4-1: ロビーにも心拍を通し、席ボードに「応答なし」を出す (v59)
a9eb893 ループバック対戦テストを固定シードで決定化する
```

## 7. 新セッションへの最初の指示(コピペ用)

> `HANDOFF_2026-07-28.md` を全文読んでから作業を始めてください。
> 別セッションが同じリポジトリを触っているので、まず `git log` と `git status` を見て、
> 知らないコミットが増えていないか確認してください。
> その後はユーザーの実機確認待ちです。§3の未確認のうち、まず v69(準備完了を
> 押し直してからの対戦開始)と v65(闘技場でCPUが落ちないか)を見てもらってください。
> 不具合が出たら、それを最優先で直してください。
