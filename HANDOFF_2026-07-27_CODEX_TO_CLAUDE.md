# カタモン Codex → Claude Code 引き継ぎ（2026-07-27）

この文書は `HANDOFF_2026-07-26.md` §14（HEAD `147a6ef` / build v40）を受けて、
Codexが行った作業と現在の残作業をまとめた差分引き継ぎである。
**以後はこの文書を最新の入口とし、背景・通信仕様の詳細は旧引き継ぎを参照すること。**

## 0. 現在地

- リポジトリ: `C:\Users\futsa\Documents\Codex\2026-07-22\claudecode\katamon`
- GitHub: `https://github.com/futsalife24-bot/katamon`
- 公開版: `https://futsalife24-bot.github.io/katamon/?v=53`
- ブランチ: `master`
- 基準コミット: `0c023be`（push済み）
- `BUILD_ID`: `v53`
- `sw.js` のキャッシュ: `katamon-pwa-v53`
- Firebase Realtime Databaseルール: Codex移管後は変更なし
- この引き継ぎ書を追加する直前の作業ツリー: クリーン

開始時:

```powershell
cd 'C:\Users\futsa\Documents\Codex\2026-07-22\claudecode\katamon'
git pull --ff-only
git status -sb
```

## 1. ユーザーとの現在の取り決め

1. 通常の `git push origin master` は毎回確認せず実行してよい。
2. force push、履歴書き換え、破壊的操作は従来どおり確認する。
3. `database.rules.json` を変える場合は、ユーザーにFirebase Consoleへ反映してもらい、**反映後にpush**する。
4. ゲームの版を上げた時は `index.html` の `BUILD_ID` と `sw.js` の `CACHE_VERSION` を同時に上げる。
5. 版を上げた時の確認URLは必ず末尾に `?v=NN` を付けて渡す。
6. 非エンジニアのユーザーなので、原因・変更・実機で見てほしい点を平易な日本語で伝える。

## 2. Codex移管後に完了したこと（v41〜v53）

### 2-1. 再戦停止の解消（v41〜v43）

旧引き継ぎ§14-0の最優先不具合は解消済み。

- `6c0b5ca` / v41: ホストが旧roundへ `lobbyState(nextRoundId)` を保存し終える前に、新roundへ切り替えないよう送信完了を待つ。
- `2a5793d` / v42: `start` を通常の物理演出FIFOから分離。前試合の死体・地形・カットインが残っていても受け取り、reveal確認中だけ専用スロットへ保留する。開始スナップショットと開始パケットの保存完了も待つ。
- `c52df5f` / v43: ゲストの `startVerifying` が初戦から残り、再戦の `start` を無言で捨てていた原因を修正。round初期化時と開始完了時に解除する。

**実機結果:** ユーザーが3戦連続の再戦成功を確認。その後も「再戦 → ロビー → 双方退出」まで成功。

### 2-2. オンライン対戦まわり

| build / commit | 内容 | 実機状況 |
|---|---|---|
| v44 `a2b9a5d` | CPU戦の中断データを持ったままオンラインへ入っても、中断データを消さない | 自動テスト済み |
| v45 `5e48ca4` | オンラインのカメラ位置を端末ごとに独立。相手の手番開始時は相手キャラへ合わせるが、片方の手動視点変更は他方へ同期しない | ユーザー報告を受け実装 |
| v46 `dce11ba` | Firebase受信分岐で落ちていた `move` を実際に適用。勝利／敗北SEを「落下…」ではなく次の勝敗画面表示時へ移動 | **移動同期を双方で実機確認済み** |
| v47 `ade4013` | ゲストがオンラインロビーからタイトルへ退出した時、ステージBGMを止めてタイトルBGMだけにする | 実装・自動テスト済み。修正後の二重BGMは未報告 |

注意: 移動パケット自体は旧作業で実装済みだったが、Firebase v3の受信ディスパッチに `move` が無く、実機では発射時まで相手が瞬間移動していた。v46で入口を通した。

### 2-3. 視点距離スライダー（v48、v49、v52）

- 戦闘中にカメラの表示距離を変更できるスライダーを追加。
- 最初の配置はユーザー意図と違ったため2回調整。
- **現在位置は下部コントローラエリア左上、必殺ボタンの反対側。**
- 端末ごとのローカルUIであり、相手端末へ値を同期しない。

最終コミット: `18081a2` / v52。
最終位置はユーザー指定どおり実装したが、v52配置後の明示的な実機OK報告はまだない。

### 2-4. 合言葉なしの簡単対戦（v50〜v51）

ユーザー判断: 大人数が同時に遊ぶ想定ではないため、合言葉は標準導線にせず、必要な時だけ使う。

- ONLINE 1V1を開くと、主ボタンは **「すぐ対戦する」**。
- 共通room `KATAMN22` の `e1` を先に取り、空ならゲスト、部屋が無ければホストとして作成。
- 同時作成競合時は再度ゲスト参加を試す。
- すでに2人が利用中なら、合言葉モードを案内する。
- **「合言葉を使う」**を押した時だけ、従来の部屋作成・参加UIを開く。
- ロビー再表示時にボタン表示を初期化する修正も追加。

コミット: `371fafe`（v50）、`1c4d514`（v51）。

制約: 簡単対戦は共通room 1室・プレイヤー2席だけ。多数組の自動マッチングキューではない。

### 2-5. ゲーム内の強制更新（v53）

- タイトル下部に **「最新版を取得」**ボタンを追加。
- 押すとService Workerの更新確認、`katamon-pwa-*` キャッシュ削除、時刻クエリ付きURLへの再起動を行う。
- 通常のService Worker登録URLも `./sw.js?build=${BUILD_ID}` に変更し、版更新を検知しやすくした。

コミット: `0c023be` / v53。
スマホでボタンが見えること・実際に旧版から更新できることは、まだユーザーの実機確認待ち。

## 3. 現在の検証結果

v53で以下をすべて実行済み。

```powershell
node tests/stage3test.js
node tests/seattest.js p1
node tests/seattest.js e1
node tests/regressiontest.js p1
node tests/regressiontest.js e1
node tests/resulttest.js
node tests/loopbacktest.js
git diff --check
```

結果:

- stage3: 173/173
- seat p1: 19/19
- seat e1: 19/19
- regression p1: 20/20
- regression e1: 20/20
- result: 26/26
- loopback: 93/93
- `git diff --check`: エラーなし（改行コード警告のみ）

## 4. 実機で確認済み／未確認

### 確認済み

- 再戦を3戦連続で実行できる。
- 双方から相手の歩行が見え、発射時の瞬間移動がなくなった。
- 再戦後にロビーへ戻り、双方が退出できる。
- 勝利／敗北SEを決着演出中ではなく勝敗画面で鳴らす要望を反映。

- **v47 / v52 / v53 の3点もユーザーが実機確認済み（2026-07-27）。**
  ゲスト退出時のBGM二重再生なし、視点距離スライダーの位置、
  「最新版を取得」による旧PWAからの更新、いずれも問題なし。

### 未確認または追加確認したい

- 画面ロック／バックグラウンド復帰、回線切替、切断後の再入室。
- 観戦者を含めた1試合完走と再戦。
- 簡単対戦の共通roomが使用中の場合の案内。

## 5. 次の大作業: 対戦ルーム専用ページ化 + BGM（Fable 5案件）

### 5-1. 以前から残っていた内容はあるか

**ある。ただし詳細仕様ではなく、方向性だけ。**

旧引き継ぎ§13-6〜13-7には次が残っている。

- 入室後のルームUI/UXを作り直す。
- 現在のポップアップではなく、**対戦ルームを専用ページ（専用シーン）化する。**
- ルーム専用BGMでタイトル・戦闘と体験を分ける。
- 画面遷移とルーム状態設計は後戻りが高くつくため、着手時にFable 5へ引き上げる。

一方で、レイアウト、演出、URLを別ページにするか、Canvas内の `gamePhase` として作るか、BGMの曲調などは未決定。

### 5-2. 現在までに確定しているUX要件

専用ページ設計では以下を前提として守ること。

1. ONLINE 1V1の標準導線は **「すぐ対戦する」**。合言葉は任意設定として奥へ置く。
2. 入室後は、参加者（P1/P2/S1/S2）、自分、プレイヤー名、キャラ選択、対戦設定、準備状態が一目で分かる。
3. キャラ選択はcommit/revealで相手に非公開。全員準備完了までは設定・選択をロックしない。準備完了は取り消せる。
4. 決着直後は勝敗を見せる。ロビーUIを被せない。対戦者は結果画面で **「このまま再戦」／「ロビーへ戻る」**を選ぶ。
5. 観戦者は選択肢を持たず、既存どおりロビーへ戻す。
6. 退出は通信不調でも端末側をタイトルへ戻す。CPU戦の中断データは保持する。
7. BGM遷移は `タイトルBGM → ルームBGM → ステージBGM → 結果 → ルームまたはタイトル`。同時に2曲鳴らさず、退出・再戦・バックグラウンド復帰も含めて状態を一元管理する。
8. Firebase v3の通信・commit/reveal・round切替は実機QAを経て安定している。専用ページ化を理由に通信プロトコルを全面改修しない。

### 5-3. Fable 5に最初に決めてもらうこと

- 専用ページを、現在の単一HTML内の新しい `gamePhase` / Canvasシーンとして作るか、DOM主体の専用画面として作るか。
- タイトルから「簡単対戦」「合言葉対戦」へ入り、待機・準備・開始・結果・再戦・退出へ至る状態遷移図。
- 2人待機時、満室時、観戦時、相手切断時、再接続時の各画面。
- 縦画面540×960での情報優先順位と、片手操作で押しやすいボタン配置。
- ルームBGMの役割、開始／停止／フェード、タイトル・ステージBGMとの責任分界。
- 簡単対戦が1室使用中の時、待機列を作るか、現状どおり合言葉へ誘導するか。
- URLの「専用ページ化」が本当に別URLを意味するか、ゲーム内専用シーンを意味するか。PWAと単一HTML構成を考えると、現時点では後者が低リスク。

### 5-4. Fable 5への依頼文（そのまま使える）

> カタモンのオンライン対戦ルームを、現在のモーダル表示から縦画面専用のルームシーンへ再設計してください。標準導線は合言葉なしの「すぐ対戦する」、合言葉は必要な時だけ開く任意機能です。入室後の参加者・自分・キャラ選択・対戦設定・準備状態を分かりやすくし、結果画面では勝敗を隠さず「このまま再戦／ロビーへ戻る」を維持してください。ルーム専用BGMを含め、タイトル→ルーム→戦闘→結果→再戦／退出の状態遷移とBGMライフサイクルを先に設計し、その後に実装してください。既存Firebase v3通信、commit/reveal、round切替は壊さず流用し、画面ロック・切断・再入室も設計に織り込んでください。実装前に画面構成案、状態遷移、失敗時の挙動、改修範囲を提示してください。

## 6. 残作業の推奨順

### P0: オンライン体験の設計を固める

1. **Fable 5で対戦ルーム専用ページ + BGMを設計。** §5の内容を使う。
2. 設計の中でStage 4（切断・画面ロック・再入室）との境界を決める。UIだけ先に作り、直後にStage 4を実装する順でもよい。

### P1: オンラインの耐久性

3. Stage 4: 切断、バックグラウンド、画面ロック、Firebase匿名Auth更新、同じ席への再入室。
4. 観戦者を含む通しQA、別回線・回線切替・長時間待機。
5. v47/v52/v53の未確認3点をスマホで確認。

### P2: 既存UIの改善

6. タイトルの最終更新日時をタップして、更新履歴を見られるようにする。
7. CPU戦の中断データがある時、CPU BATTLEボタンから目立つ再開案内を出す。
8. 出撃／発射ボタンのデザインと動きを改善。
9. タイトルの `SELECT A BATTLE MODE` を全モードの一番上へ移動。
10. 音量OFFアイコンをバツ印ではなく音符マークのまま統一し、ON/OFFは色などで示す。
11. 闘技場ステージの左右壁をマップ上端まで途切れず描画する。

### P3: マップ・キャラの新規コンテンツ

12. マップギミック追加。大橋は現在より5〜10倍長くし、途中に橋脚と着地可能な足場を設ける。
13. 目玉系の使徒風十字爆発（着弾点から上方向へ十字）。
14. 猫系の爆弾を1ターン移動不能効果へ変更。
15. 機銃系必殺: ヘリで高所へ移動し、見下ろしてジョイスティック照準、小ダメージ弾10発。
16. ワーム系: 毒ガスを2〜3ターン滞留させ、継続ダメージ。
17. ジョーカー系: DEAD LINE直下から風の影響を受けない鎌状弾を真上へ撃ち、細い縦穴を削る。

新規必殺・状態異常・特殊地形は、オンラインスナップショット、Firebaseパケット検証、再戦初期化、中断セーブの4箇所へ影響しやすい。着手前に全経路を列挙すること。

### P4: 低優先の運用確認

18. GASランキングの `character` 列を含む最新版が再デプロイ済みか確認。
19. キャラ正式名称、2vs2など旧引き継ぎの長期候補。

## 7. 実装時の重要な注意

- `index.html` は単一IIFEの大ファイル。行番号は変わるので必ず `rg` で探す。
- Firebaseの不具合は推測で直さず、「通信ログをコピー」の実機ログで入口から追う。
- Firebase v3のパケット種別を追加したら、検証、seat許可、受信dispatch、適用、ログ除外、ルール、テストの全経路を数える。
- `move` のように、送信実装があっても受信dispatchから落ちることがある。
- 再戦では前roundのフラグを全て初期化する。特に `startVerifying`、`pendingStart`、演出・物理状態。
- BGMはタイトル、ステージ、将来のルームを個別に継ぎ足さず、状態遷移から唯一の曲を決める設計が望ましい。
- PWAキャッシュで実機が旧版を掴む。build表示と「最新版を取得」を使い、報告時は双方の版を確認する。
- `database.rules.json` を変えた場合、Firebase Console反映前に新クライアントをpushしない。

## 8. 直近コミット一覧

```text
0c023be feat: add in-game update button                    v53
18081a2 fix: place view distance slider in controller area v52
1c4d514 fix: reset passcode option label on lobby reopen   v51
371fafe feat: add no-code quick online match               v50
3fc09ab fix: move view distance slider to top left         v49
f4b3f73 feat: add battle view distance slider              v48
ade4013 fix: stop stage BGM when leaving online lobby      v47
dce11ba fix: apply online movement and delay result sounds v46
5e48ca4 fix: keep online camera local to each player       v45
a2b9a5d fix: preserve CPU suspend save when playing online v44
c52df5f fix: reset guest start verification for rematches  v43
2a5793d fix: unblock Firebase rematch start                 v42
6c0b5ca fix: wait for Firebase rematch handoff              v41
447ff88 引き継ぎ書に§14(Codexへの引き継ぎ)を追記        v40
```

