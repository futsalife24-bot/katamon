# カタモン運用ルール

## プロジェクトの正本

- ローカルの正本は `C:\Users\futsa\OneDrive\デスクトップ\カタモン` とする。
- GitHubの正本は `https://github.com/futsalife24-bot/katamon` の `master` ブランチとする。
- 今後の開発・テスト・文書更新・Git操作は、原則として上記ローカルフォルダから行う。
- 旧作業場所 `C:\Users\futsa\OneDrive\デスクトップ\業務効率化\カタモン` は移行元バックアップとして扱い、更新しない。
- 旧作業場所の削除は、ユーザーの明示確認を得てから行う。

## Codexモデル・エフォート選択ルール

品質を維持しながら利用枠を効率化するため、必要な判断力を満たす最も軽いモデルを工程単位で選ぶ。

### 基本方針

- 標準はLuna Mediumとする。
- 命名、整形、一覧化、定型変換、単純な一括処理はLuna Low。
- 完成条件が明確な実装、修正、テスト、文章作成、YouTube運営補助、画像・動画・音声の定型処理はLuna Medium。
- 複数条件、広い影響範囲、曖昧な要求、複数ファイル・素材の調整はTerra Medium〜High。
- 企画、設計、戦略、仕様決定、難しい原因分析、失敗コストが高い判断はSol Medium〜High。
- Extra High〜Maxは、重大な再設計や通常設定で解決できない案件に限定する。
- 文章量、処理時間、ファイル数だけを理由に上位モデルへ上げない。
- モデルを細かな作業ごとに切り替えず、工程単位で選ぶ。

### 工程分担

原則として次の流れを優先する。

1. SolまたはTerraで方針と手順を決める
2. 明確な実行作業をLunaで行う
3. 広範囲の整合確認をTerraで行う
4. 重要判断だけSolで最終確認する

すべての工程で上位モデルを使い続けない。

### 昇格条件

LunaからTerraへ切り替える条件：

- 同じ原因で2回失敗した
- 想定より影響範囲が広がった
- 原因または完成条件が明確でない
- 複数ルールの判断が必要になった
- 元データや公開済み成果物を壊す可能性がある

TerraからSolへ切り替える条件：

- 設計、仕様、運用方針の変更が必要
- 原因候補を絞り込めない
- 復旧が難しい変更を伴う
- プロジェクトやチャンネル全体へ影響する
- Terraで2回試しても解決しない

### 安全ルール

- 指示にない全面改修、公開、送信、削除、上書きを行わない。
- 一括処理は少量のデータで検証してから全体へ適用する。
- 元ファイルを保護し、無関係なファイルを変更しない。
- 不明点を推測して重要仕様や運用方針を変更しない。
- 作業開始時に、推奨モデル・エフォート、対象、手順、完成条件を簡潔に示す。
- 完了時に、変更内容、確認結果、未確認事項、次工程の推奨モデルを報告する。
- 詳細な判断基準が必要な場合のみ`docs/model-routing-detail.md`を参照する。
